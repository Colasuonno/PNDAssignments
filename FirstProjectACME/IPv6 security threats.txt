IPv6 security threats

- IP spoofing ---> ingress filtering and RPF (reverse path forwarding)


- Covert channel (traffic class/flow label) ---> inspect packets (IDS/IPS), expected values (Traffic class:0, Flow label:0)


- Routing header, based on the extension headers (routing header in the specific: includes one or more IPs that should be visited in the path, processed by visited routers, problems with RH0,RH1 ---> RH2,RH3, RH4 are valid])

- EH Fragmentation ---> Overlapping fragments, not sending last fragment, atomic fragments) ---> not allow overlapping fragments, timer and discard for not sending, atomic fragments processed in isolation

- Bypassing RA filtering/RA-Guard ---> Do we need security tool to inspect the header chain??
	|Using any extension header
	|Using Fragment extension header



Security protocols
- Authentication header
- Encapsulating security payload

IPsec modes: tunnel mode, transport mode (can we implement one of them with the protocols above?)

Security tips for network scanning:
- Use hard to guess IIDs
	- RFC7217 better than modified EUI-64
	- RFC8064 established RFC7217 as the default
- Use IPS/IDS to detect scanning
- Filter packets where appropriate
- Be careful with routing protocols
- Use default /64 size IPv6 subnet prefix

NDP
Seems that there are a bunch of vulnerabilities in using NDP (Neighbor Discovery Protocol)
NDP is used on a link
- NS/NA spoofing (Redirection/DoS)
- Unsolicited NA (Redirection/Dos)
- NUD Failure (DoS)
- DAD (DoS attack)
- Neighbor cache attack using NS or NA
- Malicios last hop counter
- Bogus on link prefix
- Bogus address configuration prefix
- Parameter spoofing (Hop limit, DHCPv6)
- Spoofed Redirect message
- Neighbor discovery DoS attack

----> First hop security ( implemented on switches )
	- RA guard
	- IPV6 Snooping (ND inspection and DHCPv6 Snooping
	- IPv6 Source/Prefix guard
	- IPv6 Destination guard (or ND resolution rate limiter)
	- MLD Snooping
	- DHCPv6 guard

Rogue RA attack
----> use RA guard or filtering with ACL on switches

MLD (i think the professor skipped this topic)

DNS
Attacker becomes the DNS server of the victim using NDP and autoconfiguration (no further info on the slides??)

DHCPv6
Threats:
- Triggering the use of DHCP
- Rogue DHCP server
-----> DHCPv6 solutions: IPsec with encryption or end-to-end encryption?, also DHCPv6 shield